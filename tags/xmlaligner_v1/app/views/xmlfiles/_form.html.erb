  
<% form_for(@xmlfile, :url => {:action => 'update'}, :html => {:multipart => true }) do |f| %>

<!-- %= form_for(@xmlfile) do |f| %-->
  <% if @xmlfile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@xmlfile.errors.count, "error") %> prohibited this xmlfile from being saved:</h2>

      <ul>
      <% @xmlfile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">

  Filename: <%= f.text_field("name") %><br />
  Comment: <%= f.text_field("comment") %><br />
  Source Language: <%= f.text_field("source_language") %><br />
  Target Language: <%= f.text_field("target_language") %><br />
  Upload your file: <%= f.file_field("source_xmlfile") %><br />
  Upload your file: <%= f.file_field("target_xmlfile") %><br />

    <%= f.submit %>
  </div>
<% end %>
